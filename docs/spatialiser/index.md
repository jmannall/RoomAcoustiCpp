This section provides additional notes for the spatialiser.

For the main user-facing API, see:

- `Spatialiser/Interface.h` (see `interface.md`)
- `Spatialiser/Types.h` (see `../api/types.md`)
- `Spatialiser/Configs.h` (see `../api/configs.md`)
